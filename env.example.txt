# DID++ Environment Configuration
# Rename this file to .env and fill in your values

# ============ API Settings ============
API_HOST=0.0.0.0
API_PORT=8000
API_LOG_LEVEL=info

# ============ Ethereum Sepolia Blockchain ============
# Get your Alchemy API key from: https://alchemy.com
ALCHEMY_KEY=your_alchemy_api_key_here

# Smart contract addresses (deploy contracts first, then update)
DID_REGISTRY_ADDRESS=ss
VERIFICATION_LOG_ADDRESS=0x_your_verification_log_contract_address

# Wallet private key for signing transactions
# IMPORTANT: Use a testnet wallet only! Never use mainnet keys!
# Get Sepolia ETH from: https://sepoliafaucet.com
PRIVATE_KEY=your_wallet_private_key_without_0x_prefix

# Gas limit for transactions
GAS_LIMIT=300000

# ============ IPFS (Pinata) ============
# Get your Pinata keys from: https://pinata.cloud
# Option 1: Use JWT (recommended)
PINATA_JWT=your_pinata_jwt_token

# Option 2: Use API key pair
# PINATA_API_KEY=your_pinata_api_key
# PINATA_SECRET_KEY=your_pinata_secret_key

# IPFS Gateway (default is Pinata gateway)
IPFS_GATEWAY=https://gateway.pinata.cloud/ipfs

# ============ Encryption ============
# 32-byte (256-bit) master key as 64 hex characters
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
MASTER_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

# ============ Verification Settings ============
# Biometric weights (must sum to 1.0)
FACE_WEIGHT=0.40
VOICE_WEIGHT=0.35
DOC_WEIGHT=0.25

# Minimum score for successful verification
VERIFICATION_THRESHOLD=0.75

